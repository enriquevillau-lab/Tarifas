import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gruastorredeloro.tarifas',
  appName: 'Grúas Torre del Oro',
  webDir: 'dist',
  android: {
    backgroundColor: '#0f172a',
  },
};

export default config;
